# Toasty Window Analyzer

A Geode mod experiment for measuring how many frame offsets each Toasty Replay Lite input can tolerate while the run still survives.

This project intentionally refuses to display fake measurements until the TTRL event decoder and Geometry Dash checkpoint probe are connected and verified.
